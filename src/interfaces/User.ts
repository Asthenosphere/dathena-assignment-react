export interface User {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  dob: Date;
}

export interface UserDetails {
  firstName: string;
  lastName: string;
  email: string;
  dob: number;
}
